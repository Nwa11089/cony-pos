# 2026-06-02

## Sesión CONY - Comandero Web (madrugada UTC / noche MX)

### Intentos fallidos de nuevo dashboard claro
- Se intentó reemplazar `/var/www/pos+ia/clientes/cony/frontend/dashboard.html` con un diseño moderno claro con **comandero web** (botones de productos por categoría, panel lateral de pedido, +/− cantidades, confirmar pedido).
- **Problema:** El archivo HTML/JS largo se truncó al escribirse desde el editor del asistente (límite de ~26KB). El navegador cargaba JS roto → botones no funcionales.
- Se intentó reparar agregando el cierre faltante, pero el archivo quedó inconsistente.
- **Lección:** No heredoc archivos grandes de una sola pasada. Usar Python para escribir archivos largos de forma segura, o mejor aún, mantener versión de respaldo funcional.

### Solución aplicada
- **Restaurada versión oscura funcional** (con Bootstrap) del dashboard.html. Quedó operativa al instante.
- Se dejó el archivo `dashboard_claro.html` en el servidor como referencia/borrador.
- El backend (`pos_cony.py`) se actualizó para soportar la columna `emoji` en productos.
- El comando `python3 -c` para escribir archivos grandes falló por sintaxis del heredoc; en el futuro usar `sqlite3` + `curl` + `head -c` o dividir en partes.

### Pendiente
- El usuario quiere revisar el código del dashboard claro en Perplexity (otra IA) para diagnosticar el truncamiento.
- La versión funcional oscura está corriendo sin problema en `http://2.25.153.27:8200`.
# 2026-06-02

## Sesión CONY - Comandero Web (madrugada UTC / noche MX)

### Intentos fallidos de nuevo dashboard claro
- Se intentó reemplazar `/var/www/pos+ia/clientes/cony/frontend/dashboard.html` con un diseño moderno claro con **comandero web** (botones de productos por categoría, panel lateral de pedido, +/− cantidades, confirmar pedido).
- **Problema:** El archivo HTML/JS largo se truncó al escribirse desde el editor del asistente (límite de ~26KB). El navegador cargaba JS roto → botones no funcionales.
- Se intentó reparar agregando el cierre faltante, pero el archivo quedó inconsistente.
- **Lección:** No heredoc archivos grandes de una sola pasada. Usar Python para escribir archivos largos de forma segura, o mejor aún, mantener versión de respaldo funcional.

### Solución aplicada
- **Restaurada versión oscura funcional** (con Bootstrap) del dashboard.html. Quedó operativa al instante.
- Se dejó el archivo `dashboard_claro.html` en el servidor como referencia/borrador.
- El backend (`pos_cony.py`) se actualizó para soportar la columna `emoji` en productos.
- El comando `python3 -c` para escribir archivos grandes falló por sintaxis del heredoc; en el futuro usar `sqlite3` + `curl` + `head -c` o dividir en partes.

### Pendiente
- El usuario quiere revisar el código del dashboard claro en Perplexity (otra IA) para diagnosticar el truncamiento.
- La versión funcional oscura está corriendo sin problema en `http://2.25.153.27:8200`.

---

## Segunda sesión — Corte de Caja (cortes, arqueo, admin)

### Corte de caja implementado
- **Data model**: tabla `cortes` completa (estado, fondo_inicial, fechas, totales, metodos JSON, efectivo_reportado, total_arqueado, diferencia, observaciones)
- **Flujo apertura/cierre** funcional con validaciones
- **Arqueo por denominaciones**: billetes $1000/$500/$200/$100/$50/$20 + monedas + observaciones
- **Cashier oculto**: diferencia, desglose por método, total_ventas NO visibles para cajero
- **Admin panel** `/admin`: tabs Clientes, Cortes, Productos, Tiempos, Cancelaciones
  - Cortes con desglose por método + badge de diferencia
  - Productos editables
  - Clientes editables (pencil icon)
  - Cancelaciones (nueva pestaña agregada)

### Cancelaciones con autorización de admin
- **Nuevo estado 4** = "pendiente-autorizacion"
- Cajero cancela → pasa a estado 4 (NO descuenta del corte)
- Admin en Admin > Cancelaciones: Aceptar (estado 0) o Rechazar (regresa a estado 1)
- Backend valida en cierre de corte: no deja cerrar si hay estado 4
- Bitácora registra quién cancela y quién autoriza
- Fix: mensaje "Sin cancelaciones pendientes" ahora visible sobre fondo oscuro

### Bugs corregidos
- **Arqueo no registraba bien total**: inputs `type="number"` cambiados a `type="text" inputmode="numeric"` con sanitización por regex
- **Badge warning**: en admin se veía como badge amarillo sin fondo — corregido con CSS inline
- **Campo duplicado en admin_clientes**: el endpoint usaba nombre en vez de usuario — corregido

### Backup generado
- Respaldo en `/root/.openclaw/workspace/respaldos/cony/versiones/` con CHANGELOG

### Estado final de la sesión
✅ Todo implementado y funcional:
- Arqueo por denominaciones con total correcto
- Cancelaciones con autorización de admin (estado 4)
- Admin Panel: Cortes con fondo + total métodos+fondo + entregado (arqueado)
- Detalle de corte expandible con resumen + pedidos + partidas + botón reimprimir
- Bloqueo de cierre si hay cancelaciones pendientes
- Cashier nunca ve diferencias ni desglose financiero

### Pendiente
- Sistema de notificaciones al dueño (WhatsApp/email) al cerrar corte — diseñado pero no implementado
- Corrección del error "se equivocaron al dar de alta el pedido" (sin cebolla, etc.) — pendiente para otro mensaje
- Reimpresión de ticket 🖨️ (placeholder, backend pendiente)
## 2026-06-02 — CONY POS Avances

### Cambios realizados

#### Hot dog hawaiano vs suizo (separados)
- `Hot dog suizo o hawaiano` → separado en **Hot dog hawaiano** (id=3, con Piña) y **Hot dog suizo** (id=27, con Champiñones)
- `Hamburguesa hawaiana o suiza` → separado en **Hamburguesa hawaiana** (id=7, con Piña) y **Hamburguesa suiza** (id=28, con Champiñones)
- Categoría `Hot dogs especiales` corregida: Crazy ($90), Sucio ($95), Descerebrado ($100), La Bombón ($115) ahora visibles en el filtro del comandero

#### Workflow de personalización
- **PASO 1**: El modal ahora muestra primero "✅ Con todo" / "✏️ Editar"
- **"Con todo"**: Agrega directo al carrito con nota "Con todo" + comentario opcional
- **"Editar"**: Abre el modal completo con ingredientes toggle (rojo=incluido, gris=quitado)
- **Nota rápida**: Campo de observaciones visible en ambos pasos (partido a la mitad, etc.)
- **Productos con salsa (alitas, boneless)**: "Con todo" redirige automáticamente al editor para elegir salsa
- Función `agregarCarrito` reemplazada por nuevo workflow de dos pasos
- Funciones: `selectorConTodo()`, `selectorEditar()`, `volverAccion()`, `confirmarPersonalizar()`

#### Botón "Ver detalle" en Últimos pedidos
- Cada fila de pedido tiene botón 👁️
- Modal de detalle muestra: cliente, teléfono, total, método de pago, cada producto con su personalización (📝 "Con todo" o "Sin: Cebolla | Salsa: Buffalo")
- Modal de detalle: `verDetallePedido(folio)`

#### Healthcheck y hardening del VPS
- **UFW activado**: Solo SSH (22→22022), CONY (8200), otro (8000) abiertos; bloqueo por defecto
- **SSH migrado a puerto 22022**: Config en `/etc/ssh/sshd_config.d/01-custom-port.conf`
- Gateway OpenClaw (loopback) y CONY POS intactos

#### Productos actualizados
- Corrección de precios y complementos en hot dogs y hamburguesas
- Complementos asignados correctamente por producto

### Pendiente
- **Diseño de impresión** → pausado, Neto consultará su sistema de escritorio para ejemplos
- Notificaciones al dueño
- Landing page de CONY POS
- Subir a GitHub

### Notas técnicas
- Dashboard: `/var/www/pos+ia/clientes/cony/frontend/dashboard.html`
- Backend: `/var/www/pos+ia/clientes/cony/backend/pos_cony.py`
- DB: `/var/www/pos+ia/clientes/cony/db/cony.db`
- Usuarios: superuser(1234), admin(1234), cajero1(1234), esme(1234), neto(1234)
- VPS IP: 2.25.153.27:8200 (puerto SSH cambiado a 22022)
